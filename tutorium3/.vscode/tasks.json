{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "build: composition",
      "type": "shell",
      "command": "make",
      "args": ["composition"],
      "options": { "cwd": "${workspaceFolder}" },
      "group": "build",
      "problemMatcher": ["$gcc"]
    }
  ]
}
