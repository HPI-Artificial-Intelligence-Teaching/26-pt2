{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "build: token_parsing",
      "type": "shell",
      "command": "make",
      "args": ["token_parsing"],
      "options": { "cwd": "${workspaceFolder}" },
      "group": "build",
      "problemMatcher": ["$gcc"]
    },
    {
      "label": "build: rule_of_three",
      "type": "shell",
      "command": "make",
      "args": ["rule_of_three"],
      "options": { "cwd": "${workspaceFolder}" },
      "group": "build",
      "problemMatcher": ["$gcc"]
    }
  ]
}
